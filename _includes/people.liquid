<div class="col">
  <a href="{% if people.redirect %}{{ people.redirect }}{% else %}{{ people.url | relative_url }}{% endif %}">
    <div class="card h-100 shadow-none border-0 bg-transparent"> {% comment %} remove the weird dropshadow {% endcomment %}
      {% if people.img %}
        {%
          include figure.liquid
          loading="eager"
          path=people.img
          sizes = "250px"
          alt="people thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body px-0 pt-0 pb-0">
        <h4 class="card-title mt-0 mb-1">{{ people.title }}</h4>
        <p class="card-text">{{ people.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if people.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ people.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if people.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ people.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
